视觉助手：面向视障人士的移动障碍物检测与语音导航系统
1. 系统概述
本系统是一款专为视障人士设计的边缘智能辅助穿戴设备。系统针对视障人士在复杂户外环境下的出行痛点，创新性地提出了一种**“端-边-云”协同的分布式架构**。
系统以 MaixCam 作为纯视觉感知边缘节点，以树莓派作为语音交互与调度的中枢大脑，结合轻量级大语言模型（LLM）进行自然语言意图理解，并利用智能手机作为宏观导航的代理节点。此外，系统配备了完整的云端 Web 监控大屏，用于实时采集用户的出行轨迹与系统状态。

2. 系统总体架构设计
系统总体划分为四大逻辑层：

视觉感知层 (MaixCam)： 负责高帧率的实时路况图像采集、障碍物检测与单目测距。

中枢控制层 (树莓派)： 负责音频采集、端点检测（VAD）、意图提取、跨设备调度以及 GPS 数据读取。

导航代理层 (智能手机)： 作为免开发的“黑盒”黑盒节点，负责利用成熟商业 API（高德/百度）完成宏观的路径规划和语音播报。

云端监控层 (Web 后端)： 负责接收端侧上报的时序数据，进行可视化展示。

3. 硬件选型与物理连接
视觉运算节点： MaixCam (搭载 K230 NPU)

主控中枢节点： 树莓派 4B / 5 (运行 64位 Raspberry Pi OS)

辅助传感器： * USB 麦克风 (用于捕捉用户语音指令)

串口 GPS 模块 (如 ATGM336H，通过 GPIO 连接树莓派，用于采集真实运动轨迹)

数据通信链路：

MaixCam 与树莓派：通过 USB 线缆直连，配置为虚拟网卡 (USB-Ethernet) 模式，使用 Socket (TCP/IP) 进行低延迟、高带宽的本地数据通信，同时解决 MaixCam 的供电问题。

树莓派与手机：通过低功耗蓝牙 (BLE) 或热点局域网通信。

树莓派与云端：通过 4G/Wi-Fi 网络使用 HTTP POST/MQTT 协议进行数据上报。

4. 核心功能模块实现
4.1 基于 YOLO11 的环境感知与预警模块 (MaixCam)
模型选型： 采用经过 INT8 量化编译的 YOLO11n 轻量级目标检测模型，兼顾 NPU 极低推理延迟（约 10-30ms）与高精度。

白名单与优先级过滤： 抛弃全局无差别播报，在代码层面构建 OBSTACLE_WHITELIST（包含车辆、消防栓、行人等），并映射 1~4 级的危险优先级。每一帧只提取当前画面中“优先级最高且距离极近”的高危目标。

单目空间测距： 基于小孔成像模型与先验物理高度（如轿车 1.5m，消防栓 0.8m），通过目标在画面中的像素高度与相机焦距反算深度距离。

4.2 基于大模型的自然语言意图抽取模块 (树莓派)
智能录音 (VAD)： 抛弃传统能量阈值检测，引入工业级 Silero VAD 深度学习模型，精准滤除户外风声与车流噪声音，实现毫秒级的语音端点截断。

意图理解 (LLM Slot Filling)： 部署仅 26M 参数的 MiniMind 语言模型。使用包含 ASR 错别字和口语冗余噪音的定制 SFT 数据集对其进行微调，使其能够从自然语言中稳定输出结构化 JSON，例如 {"intent": "navigate", "target": "地铁站"}。

4.3 跨设备协同导航模块
微观避障 (本地接管)： 若意图为 find_object，树莓派通过 Socket 将目标发送给 MaixCam，MaixCam 锁定目标后回传相对方位与距离，由树莓派 TTS 播报。

宏观导航 (手机代理)： 若意图为 Maps，树莓派利用 URL Scheme (如 amapuri://route/plan/?dname=目的地) 或蓝牙 HFP 协议触发手机地图 App，全自动拉起高德步行导航，语音通过用户的蓝牙耳机播放。

4.4 Web 后端与数据可视化看板
技术栈： 基于 FastAPI (Python) 构建高并发异步接口，SQLite 作为本地轻量化关系型数据库，前端使用 Vue.js + ECharts。

核心监控指标：

视障用户轨迹图： 接收树莓派 GPS 模块的经纬度数据，在前端地图控件上绘制连续的出行轨迹。

障碍物高频特征统计： 统计不同类型路障的出现频率（饼图展示），为城市无障碍设施建设提供数据支持。

设备健康度监控： 实时折线图展示 MaixCam / 树莓派的 CPU 占用率与内存消耗。

5. 项目创新点与论文亮点提炼
极度轻量化的端侧大模型应用： 突破了传统设备只能依靠云端 API 进行语义理解的局限，成功在树莓派 CPU 上离线部署并微调了 MiniMind 大语言模型，极大提升了断网环境下的可用性与隐私安全性。

视觉与中枢的解耦与协同： MaixCam 专注视觉 NPU 加速，树莓派负责高层逻辑与 LLM 调度，彻底解决了单一嵌入式设备算力瓶颈与线程阻塞问题。

非侵入式（黑盒）导航代理设计： 避免了繁重且闭源的商业地图 SDK 移动端二次开发，通过系统级协议调用手机导航，同时利用独立 GPS 模块在后端保留了完整的数据监控能力，兼顾了用户体验与工程数据的完整性。