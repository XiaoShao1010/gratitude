阶段一：视觉与通信基座打通（预计耗时：1 周）
目标： 让 MaixCam 和树莓派成功“握手”，建立稳定且低延迟的双向数据流。

硬件连接与网络配置：

用 USB 线连接 MaixCam 与树莓派。

在树莓派系统（Raspberry Pi OS）中配置虚拟网卡（通常为 usb0），确保能 ping 通 MaixCam 的默认 IP（如 192.168.233.1）。

重构 MaixCam 端代码 (Socket Client)：

在现有的 main.py 中引入 socket 模块。

发送链路： 每帧或每 0.5 秒，将视野中“优先级最高”的危险障碍物打包成 JSON（如 {"type": "warning", "object": "car", "distance": 1.2}）发送出去。

接收链路： 开启一个非阻塞的接收线程，随时监听树莓派发来的寻找指令，动态更新 target_to_find 变量。

编写树莓派中枢骨架 (Socket Server)：

在树莓派上用 Python 建立一个 TCP Server 监听端口。

编写基础的语音合成（TTS）函数（如使用 pyttsx3 或边沿端语音引擎）。收到 MaixCam 的警告 JSON 时，立刻触发语音播报：“注意，前方 1.2 米有汽车”。

里程碑 A： 拿着连接着树莓派的 MaixCam 走动，能听到树莓派实时播报危险障碍物。

阶段二：部署智能语音与意图大脑（预计耗时：1-2 周）
目标： 让树莓派具备“听懂复杂指令”的 AI 能力。

集成 Silero VAD 智能录音：

在树莓派安装 PyTorch 与 Torchaudio。

编写麦克风持续监听代码，利用 Silero VAD 精准截取用户的纯净说话音频，丢弃环境噪音。

接入语音转文字 (STT)：

为了快速推进，初期建议直接调用百度或阿里的免费短语音识别 API，将 VAD 截取出的音频转为中文字符串。

MiniMind 本地部署与调度：

将在电脑上用专业数据集微调好的 minimind_assistant_final 权重文件夹拷贝到树莓派。

编写大模型推理脚本，输入 STT 的中文字符串，输出结构化意图（intent 和 target）。

闭环“寻找”功能：

当 LLM 解析出 {"intent": "find_object", "target": "apple"} 时，树莓派通过 Socket 将 "apple" 发给 MaixCam。

里程碑 B： 对着树莓派说“帮我找一下苹果”，MaixCam 屏幕上的绿框自动锁定苹果，树莓派播报“苹果在左边，约 0.5 米”。

阶段三：宏观导航与手机代理协同（预计耗时：1 周）
目标： 解决户外长距离导航规划问题。

确定触发协议：

选择“URL Scheme 唤醒法”。在树莓派与用户的智能手机之间建立基础连接（可以使用简单的局域网 HTTP 请求，要求手机开热点给树莓派；或使用 BLE 广播）。

手机端极简接收器开发：

利用 Python (Kivy) 或 Android Studio 编写一个只有后台监听功能的简易 App。

收到树莓派发来的目的地字符串后，执行系统级 Intent，拉起 amapuri://route/plan/?dname=目的地&t=4。

意图路由分发：

在树莓派代码中增加逻辑分支：如果 LLM 输出 intent: navigate，则调用导航触发函数，并暂停 MaixCam 的“寻找”模式（恢复到默认的安全预警模式）。

里程碑 C： 对着树莓派说“导航去地铁站”，手机屏幕亮起，自动打开高德地图并开始播报步行路线。

阶段四：数据沉淀与 Web 监控大屏（预计耗时：1-2 周）
目标： 完成论文所需的工程数据采集与可视化支撑。

硬件拓扑升级 (GPS 接入)：

购买串口 GPS 模块（如 ATGM336H），连接树莓派的 TX/RX 引脚。

在树莓派上编写一个独立的后台守护线程，每 5 秒读取一次真实经纬度。

搭建云端 Web 后端：

使用 FastAPI 编写 RESTful 接口（/upload_gps, /upload_log）。

使用 SQLite 设计数据库表结构（轨迹表、障碍物高频日志表、设备状态表）。

树莓派数据上报逻辑：

树莓派在运行过程中，异步将“遭遇的 P1 级障碍物”、“当前经纬度”以 HTTP POST 发送至云端。

前端大屏开发：

使用 Vue.js + ECharts，实现一张动态地图（展示盲人轨迹）和几个核心统计图表（障碍物饼图、意图识别耗时折线图）。

里程碑 D： 带着设备在校园走一圈，打开电脑浏览器，能看到一条完整的运动轨迹和遇到的障碍物散点记录。

阶段五：系统联调、压测与论文撰写（预计耗时：2 周）
目标： 消除系统集成的 Bug，收集客观测试数据，形成学术产出。

极限边缘测试：

模拟复杂环境：在马路边（高噪音）测试 VAD 的截断准确率；在强光/逆光下测试 MaixCam 的识别率。

性能 Profiling (核心数据收集)：

记录系统各环节的具体耗时（如：视觉单帧推理耗时 XX ms，LLM 意图抽取耗时 XX s，系统整体端到端延迟 XX s）。这些硬核数据是毕业论文“系统测试与性能分析”章节的灵魂。

代码清理与注释：

规范化函数命名，按模块拆分代码库，补充必要的异常捕获（try-except），防止网络波动导致整个系统崩溃。